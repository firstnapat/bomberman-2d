[package]
name = "bomberman-2d"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.15", default-features = false, features = ["bevy_winit", "bevy_render", "bevy_sprite", "bevy_webgl2"] }

[dependencies.web-sys]
version = "0.3"

[lib]
crate-type = ["cdylib", "rlib"]

