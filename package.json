{
  "name": "bomberman-2d",
  "version": "0.1.0",
  "description": "Bomberman 2D game built with Bevy",
  "type": "module",
  "scripts": {
    "build": "wasm-pack build --target web --release --out-dir pkg && cp index.html pkg/",
    "dev": "wasm-pack build --target web --dev --out-dir pkg && cp index.html pkg/ && node server.js",
    "dev:watch": "cargo watch -x 'wasm-pack build --target web --dev --out-dir pkg' -s 'cp index.html pkg/'"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
